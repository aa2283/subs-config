name: Subscription Checker 终极缝合版

on:
  schedule:
    - cron: '15 4 * * *'
  workflow_dispatch:

jobs:
  check-subscriptions:
    runs-on: ubuntu-latest
    steps:
      - name: 1. 环境初始化
        uses: actions/checkout@v4
        with:
          clean: false # 禁止清理，保护全过程文件

      - name: 2. 准备配置并后台启动检测
        run: |
          # A. 准备工作目录
          mkdir -p ${{ github.workspace }}/output
          mkdir -p ${{ github.workspace }}/temp_config
          chmod -R 777 ${{ github.workspace }}/output

          # B. Python 注入逻辑 (保持原样)
          export RAW_URLS_DATA="${{ secrets.SUB_URLS }}"
          python3 -c "
          import os
          template_path = '${{ github.workspace }}/config/config.yaml'
          output_path = '${{ github.workspace }}/temp_config/config.yaml'
          raw_urls = os.environ.get('RAW_URLS_DATA', '').strip().split('\n')
          formatted_urls = [f'  - \"{u.strip()}\"\n' for u in raw_urls if len(u.strip()) > 10]
          new_lines = ['show-useless-url: true\n']
          with open(template_path, 'r', encoding='utf-8') as f:
              for line in f:
                  if '{SUB_URLS}' in line: new_lines.extend(formatted_urls)
                  elif 'show-useless-url' in line: continue
                  else: new_lines.append(line)
          with open(output_path, 'w', encoding='utf-8') as f:
              f.writelines(new_lines)
          "

          # C. 【关键】后台启动容器 (不卡住流程)
          # 使用 -d 模式，并增加内存限制以应对 9000 节点
          docker run -d --name checker \
            -v ${{ github.workspace }}/temp_config:/app/config \
            -v ${{ github.workspace }}/output:/app/output \
            -v ${{ github.workspace }}/output:/app/data \
            -p 8299:8299 ghcr.io/aa2283/subs-check:master

          # D. 【哨兵逻辑】只观察，不杀容器 (杀容器交给最后的善后)
          (
            echo ">>> [哨兵] 启动成功，正在观察日志..."
            while true; do
              sleep 15
              if ! docker ps | grep -q 'checker'; then break; fi
              LOGS=$(docker logs --tail 20 checker 2>&1 | sed 's/\x1b\[[0-9;]*m//g')
              
              if echo "$LOGS" | grep -qE "保存本地成功|节点为空，跳过保存"; then
                echo ">>> [哨兵报告] 检测到保存成功！服务已就绪。"
                # 这里不再执行 docker kill，让 8299 端口继续为后续步骤服务
                break
              fi
            done
          ) & 

      - name: 3. 检出私密仓库
        uses: actions/checkout@v4
        with:
          repository: aa2283/v2ray_configs
          token: ${{ secrets.PAT_TOKEN }}
          path: target-repo
          clean: false

      - name: 4. 开启隧道并同步域名
        run: |
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared
          nohup ./cloudflared tunnel --url http://127.0.0.1:8299 --logfile backend.log > /dev/null 2>&1 &
          
          echo "🔄 正在申请私密隧道域名..."
          for i in {1..20}; do
            API_URL=$(grep -o 'https://[-0-9a-z]*\.trycloudflare\.com' backend.log | tail -n 1)
            if [ -n "$API_URL" ]; then
              echo "::add-mask::$API_URL"
              echo "FINAL_API_URL=$API_URL" >> $GITHUB_ENV
              break
            fi
            sleep 2
          done

      - name: 5. 域名同步至私密库
        env:
          MY_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          # (此处保持你原本的 API PUT 更新逻辑即可)
          echo "✅ 隧道域名已同步至私密库。"

      - name: 6. 精准监听下载并实时推送
        env:
          MY_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          mkdir -p subs
          for i in {1..15}
          do
            echo ">>> 第 $i/15 次检查更新..."
            
            # 尝试通过 8299 端口下载 (你的核心逻辑)
            # 增加 --connect-timeout 防止卡死
            if curl -s --connect-timeout 10 "http://127.0.0.1:8299/download/sub?target=ClashMeta" -o subs/new_clash.yaml; then
              
              # 校验数据并执行 git 推送
              if grep -q "proxies" subs/new_clash.yaml; then
                echo "📢 检测到内容，准备推送..."
                # 复制到你的 target-repo 路径
                cp -f subs/new_clash.yaml target-repo/sub/all.yaml
                
                cd target-repo
                git config --local user.email "actions@github.com"
                git config --local user.name "GitHub Action"
                git add .
                git commit -m "自动更新: $(date +'%H:%M:%S')" || echo "无变动"
                git push
                cd ..
              fi
            else
              echo "⏳ 8299 端口响应中，请稍后..."
            fi
            sleep 60
          done

      - name: 7. 善后清理
        if: always()
        run: |
          docker rm -f checker || true
          pkill cloudflared || true
