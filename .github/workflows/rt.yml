name: Subs-Check 严格流水线版

on:
  schedule:
    - cron: '15 4 * * *'
  workflow_dispatch:

jobs:
  check-subscriptions:
    runs-on: ubuntu-latest
    steps:
      - name: 1. 检出代码
        uses: actions/checkout@v4

      - name: 2. 准备配置文件
        run: |
          mkdir -p ${{ github.workspace }}/output ${{ github.workspace }}/temp_config ${{ github.workspace }}/rename
          chmod -R 777 ${{ github.workspace }}/output
          export RAW_URLS_DATA="${{ secrets.SUB_URLS }}"
          python3 -c "
          import os
          template_path = '${{ github.workspace }}/config/config.yaml'
          output_path = '${{ github.workspace }}/temp_config/config.yaml'
          raw_urls = os.environ.get('RAW_URLS_DATA', '').strip().split('\n')
          formatted_urls = [f'  - \"{u.strip()}\"\n' for u in raw_urls if len(u.strip()) > 10]
          
          # 核心修复：限制并发，关闭自动检测(cron: '')，确保我们可以手动或有序触发
          new_lines = [
              'web-panel: true\n', 
              'api-port: 8199\n', 
              'sub-store-port: 8299\n',
              'concurrent: 120\n',
              'watch: false\n',
              'cron: \"\"\n'
          ]
          with open(template_path, 'r', encoding='utf-8') as f:
              for line in f:
                  if any(x in line for x in ['web-panel', 'api-port', 'sub-store-port', 'concurrent', 'watch', 'cron']): continue
                  if '{SUB_URLS}' in line: new_lines.extend(formatted_urls)
                  else: new_lines.append(line)
          with open(output_path, 'w', encoding='utf-8') as f:
              f.writelines(new_lines)
          "

      - name: 3. 【第一步】启动 8299 基础环境
        run: |
          echo "🚀 启动后端容器 (8299优先)..."
          docker run -d --name checker \
            -p 8199:8199 -p 8299:8299 \
            -v ${{ github.workspace }}/temp_config:/app/config \
            -v ${{ github.workspace }}/output:/app/output \
            ghcr.io/aa2283/subs-check:master

          echo "⏳ 等待 8299 端口响应..."
          for i in {1..20}; do
            if curl -s http://127.0.0.1:8299 > /dev/null; then
              echo "✅ Sub-Store 已就绪"
              break
            fi
            sleep 2
          done

      - name: 4. 【第二步】在空闲网络下申请隧道
        env:
          MY_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          echo "📥 下载并建立分流隧道..."
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared

          # 分流：域名/ 为 8299，域名/check 为 8199
          cat <<EOF > tunnel.yml
          ingress:
            - hostname: "*"
              path: /check
              service: http://localhost:8199
            - hostname: "*"
              service: http://localhost:8299
          EOF

          nohup ./cloudflared tunnel --config tunnel.yml run --url > tunnel.log 2>&1 &
          
          echo "🔄 捕获域名并上报私有库..."
          for i in {1..20}; do
            API_URL=$(grep -o 'https://[-0-9a-z]*\.trycloudflare\.com' tunnel.log | tail -n 1)
            if [ -n "$API_URL" ]; then
              echo "::add-mask::$API_URL"
              echo "FINAL_API_URL=$API_URL" >> $GITHUB_ENV
              SHA=$(curl -s -H "Authorization: token $MY_TOKEN" "https://api.github.com/repos/aa2283/v2ray_configs/contents/tunnel_url.txt" | grep -o '"sha": "[^"]*' | cut -d'"' -f4 || echo "")
              CONTENT=$(echo -n "$API_URL" | base64 | tr -d '\n')
              curl -s -X PUT -H "Authorization: token $MY_TOKEN" -H "Content-Type: application/json" \
                -d "{\"message\":\"Update API\",\"content\":\"$CONTENT\",\"sha\":\"$SHA\"}" \
                "https://api.github.com/repos/aa2283/v2ray_configs/contents/tunnel_url.txt"
              echo "✅ 隧道已绑定: $API_URL"
              break
            fi
            sleep 3
          done

      - name: 5. 【第三步】开始执行 8199 节点检测
        run: |
          echo "📢 网络环境已稳固，现在开始 8996 节点大规模检测..."
          # 这里通过监控日志来等待检测完成
          while true; do
            LOGS=$(docker logs --tail 10 checker 2>&1 | sed 's/\x1b\[[0-9;]*m//g')
            echo "$LOGS" | grep "进度:" | tail -n 1 || true
            
            if echo "$LOGS" | grep -q "保存本地成功"; then
              echo "✅ 检测 100% 完成并存盘！"
              break
            fi
            sleep 15
          done

      - name: 6. 【第四步】利用 8299 重命名节点
        run: |
          echo "📥 正在调用 Sub-Store 接口执行重命名转换..."
          # 下载重命名后的文件
          curl -s "http://127.0.0.1:8299/download/rename?target=URI" -o "${{ github.workspace }}/rename/URI.txt"
          curl -s "http://127.0.0.1:8299/download/rename?target=ClashMeta" -o "${{ github.workspace }}/rename/ClashMeta.yaml"
          echo "✅ 重命名完成。"

      - name: 7. 【第五步】推送数据至 GitHub
        uses: actions/checkout@v4
        with:
          repository: aa2283/v2ray_configs
          token: ${{ secrets.PAT_TOKEN }}
          path: target-repo

      - name: 8. 执行 Git 推送 (修复权限版)
        run: |
          # 1. 强制修正 output 文件夹权限，确保 Action 用户可以读取
          sudo chown -R $USER:$USER ${{ github.workspace }}/output
          sudo chmod -R 755 ${{ github.workspace }}/output
          
          cd target-repo
          mkdir -p sub rename
          
          # 2. 清理旧文件
          rm -rf sub/* rename/*
          
          # 3. 拷贝新文件 (增加 -f 强制覆盖，并排除掉报错的 log 文件以防万一)
          # 我们通常不需要推送日志文件到 GitHub
          cp -rv ../output/* sub/ --exclude=*.log || cp -rv ../output/* sub/
          cp -rv ../rename/* rename/
          
          # 4. Git 提交
          git config --local user.email "actions@github.com"
          git config --local user.name "Action"
          git add .
          
          # 检查是否有文件变动，防止空提交报错
          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "Update Nodes $(date +'%Y-%m-%d %H:%M')"
            git push
          else
            echo ">>> 无内容变动，跳过推送。"
          fi

      - name: 9. 【第六步】最后挂机 15 分钟
        run: |
          echo "========================================================="
          echo "🎉 所有自动化任务已完成！"
          echo "🌍 调试地址 (Sub-Store): ${{ env.FINAL_API_URL }}"
          echo "📊 结果面板 (Checker): ${{ env.FINAL_API_URL }}/check"
          echo "🕒 容器将保持在线 15 分钟供您手动调试..."
          echo "========================================================="
          for i in {1..15}; do
            echo ">>> [挂机中] 第 $i/15 分钟"
            sleep 60
          done
          docker kill checker || true
